
  It all starts with docker

* Benefits of contenerization
* Docker "need to know":
  * `docker run`
  * `docker run -p`
  * `docker run -v`
  * `docker run -e`
  * `docker exec`
  * `docker secret create`
  * `docker run -u`
