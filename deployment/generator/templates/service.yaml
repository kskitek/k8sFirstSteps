apiVersion: v1
kind: Service
metadata:
  name: {{ include "generator.fullname" . }}
  labels:
    component: generator
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    component: generator
